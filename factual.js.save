const fs = require('fs')

let text;  
fs.readFile('resources/data.txt', (err, data) => {
	if (err){
		 throw err;
	}
	text=data.toString();
	text=text.replace("\n","");
	console.log(text);
})
axios.get('https://site.com/', {
  params: {
    foo: 'bar'
  }
})
